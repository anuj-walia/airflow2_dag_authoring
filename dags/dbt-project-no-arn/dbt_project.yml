name: 'optm_eu_reader'
version: '1.0.0'
config-version: 2
profile: 'optm_eu_reader'

# Paths
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

# Global model configs
models:
  optm_eu_reader:
    +materialized: view

vars: {}
